# MCP 注册表

> 本文件由 `tools/registry/refresh_manifest_md.py` 根据 `mcps/registry.json` 生成。

## 本地回声 MCP

- ID：`mcp_e82d00f10cfb`
- 启用：是
- 传输：`stdio`
- 描述：一个用于演示和测试的本地标准输入输出 MCP，包含 echo 工具，会把传入参数原样返回。适合验证 load_mcp 和 use_tool 的基础链路。
- 备注：仅用于 demo 和测试，不提供真实业务能力。
