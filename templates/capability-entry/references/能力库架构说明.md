# 能力库架构说明

能力库采用“入口前置、细节按需读取”的结构。

平台只自动发现 `capability-entry/SKILL.md`。用户能力组保存在 `skills/`，MCP 配置保存在 `mcps/registry.json`，通用调用程序保存在 `tools/`。

`capability-entry/`、`skills/`、`mcps/registry.json` 和兼容入口 `CAPABILITY.md` 属于用户数据。更新器只按文件更新通用程序和一般性文档，不覆盖这些用户文件。
